import { DegreePrefixPipe } from './degree-prefix.pipe';

describe('DegreePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new DegreePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
