export interface NFTValue {
    id: number,
    title?: string,
    volume?: number,
    day_change: number,
    week_change: number,
    floor_price?: number,
    owners?: number,
    assets?: number
}